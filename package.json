{
  "name": "ccip-starter-kit-hardhat",
  "scripts": {
    "compile": "hardhat compile",
    "generate": "npx wagmi generate",
    "test": "hardhat test",
    "test:fork": "npx hardhat test --grep 'Integration'",
    "test:no-fork": "npx hardhat test --grep '^(?!.*Integration).*$'"
  },
  "devDependencies": {
    "@chainlink/contracts-ccip": "1.4.0",
    "@chainlink/env-enc": "^1.0.5",
    "@chainlink/local": "^0.2.1",
    "@nomicfoundation/hardhat-ethers": "^3.0.8",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/hardhat-upgrades": "^3.2.1",
    "@wagmi/cli": "^2.1.15",
    "ethers": "^6.13.2",
    "hardhat": "^2.22.7"
  }
}